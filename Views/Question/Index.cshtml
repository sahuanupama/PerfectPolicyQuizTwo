@model IEnumerable<PerfectPolicyQuizTwo.Models.QuestionModel.Question>

@{ ViewData["Title"] = "Questions"; }

<h1>Questions</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.questionId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.questionToipc)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.questionText)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.questionImage)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.quizId)
            </th>
            @*<th>
                    @Html.DisplayNameFor(model => model.options)
                </th>*@
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
<tr>
    <td>
        @Html.DisplayFor(modelItem => item.questionId)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.questionToipc)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.questionText)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.questionImage)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.quizId)
    </td>
    @*<td>
            @Html.DisplayFor(modelItem => item.options)
        </td>*@
    <td>
        @Html.ActionLink("Edit", "Edit", new { id = item.questionId }) |
        @Html.ActionLink("Details", "Details", new { id = item.questionId }) |
        @Html.ActionLink("Delete", "Delete", new { id = item.questionId })
    </td>
</tr>
}
    </tbody>
</table>
